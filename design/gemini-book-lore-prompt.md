# Gemini prompt — Get Started book lore pass

Attach/paste `get-started-book-content-draft-v1.md` along with the message below.

---

This is a first draft of a Skyrim-themed "Get Started" book for a Vortex mod-collection management
app. Every tool has a locked Skyrim name already (naming principle: a Skyrim term whose in-world
meaning maps to what the tool actually does — see the "why these names" reasoning baked into each
entry).

Please rewrite the **Lore** paragraph for each tool — keep the same voice and length as the current
drafts (2-4 sentences, in-world, no plain-language explanation mixed in), but give me your own
independent take rather than just polishing my wording. Leave the **"What it actually does"** boxes
completely untouched — those are already final.

Two tools — **The Artisan's Log** and **The Sanctuary** — are flagged as draft *names*, not just
draft lore. If you think a different Skyrim term fits either tool's real function better, say so and
suggest a replacement name, not just new lore for the current one.
