{
  "project": "vortex-collection-tools",
  "inFlight": null,
  "items": [
    {
      "id": "vortex-running-retry-broken-other-pages",
      "tag": "bug",
      "title": "Normalize Vortex-running handling: real modal everywhere, fix the broken retry too",
      "why": "Director's own call: the real modal (with a working Try Again) is the standard everywhere else in this app -- Rules Generator / Rules Generator Report instead show a plain grey inline line for the same 'Vortex must be closed' condition, easy to miss and inconsistent. Convert those to the real modal, and fix the same broken-retry bug already found on 4 files while in there.",
      "prompt": "Follow-up, found and explicitly flagged (not fixed) while fixing the same bug on Rebuild Missing Files (commit b3eb0c7, queue: rmf-vortex-running-retry-broken). Confirmed via `grep -n \"showVortexRunningModal\" web/public/*.js`: the SAME hardcoded-`() => {}` empty-retry bug exists in FOUR other files -- `cleanup-app.js`, `missing-masters-app.js`, `reports-rulesgen-app.js`, `rules-generator-app.js`. Only `app.js`, `merge-app.js` (fixed 2026-07-28), and now `rebuild-missing-app.js` do this correctly.\n\nSYMPTOM (same as the already-fixed case): if any of these four pages' own actions hits a 'Vortex is running' 409 while Vortex is open, the modal shows correctly, but clicking Try Again after closing Vortex does nothing -- no retry fires. The only recovery is navigating away and starting over, on whichever page hits this.\n\nSCOPE: for EACH of the four files, find every call site of `showVortexRunningModal` (same `grep` as above), give the shared error handler (or wherever the modal gets triggered) an optional `retryFn` parameter, and thread the REAL retry action through each call site -- same pattern as `rebuild-missing-app.js`'s own `rmfHandleError(e, box, retryFn)` (b3eb0c7), don't reinvent the approach. Some call sites may genuinely have nothing sensible to retry (e.g. a background/informational check) -- it's fine to leave those as an intentional no-op, but say which ones and why in the handoff rather than leaving it ambiguous.\n\nVERIFY LIVE, at least one real repro per file (not just code-reasoned) -- trigger a real 'Vortex is running' 409 on each of the four pages' own most likely action (e.g. Vortex Scrub's own scan, Missing Masters' own scan, Rules Generator's own apply/plan step), close Vortex, click Try Again, confirm the action actually retries and completes -- not just that the modal closes. Handoff with what you tested per file.\n\n--- SCOPE CORRECTION, director's own explicit direction (2026-08-15) ---\n\nDirector checked Rules Generator Report live: instead of the modal, its 'new collection' dropdown shows a plain grey `.muted` inline line (\"Close Vortex to load the list of new collections.\") when Vortex is open -- no modal, not even warning-colored. Director's own call: **the real modal is the standard everywhere else in this app, and that's what should show here too, not a quiet grey line.** \"bottom line if Vortex is open we should do the modal, pop-up not a single simple line.\"\n\nREVISED SCOPE -- for EACH of the four files (`cleanup-app.js`, `missing-masters-app.js`, `reports-rulesgen-app.js`, `rules-generator-app.js`), do BOTH of these, not just the retry fix:\n1. Fix the broken retry on every EXISTING `showVortexRunningModal` call site (as originally scoped above).\n2. AUDIT each file for any OTHER place that currently shows Vortex-must-be-closed as a plain inline/muted line instead of the real modal (the Rules Generator Report dropdown is the confirmed real example -- check the other three files for the same pattern, don't assume only Rules Generator has it) -- convert those to use the real `showVortexRunningModal(retryFn)` instead, with a real working retry wired the same way as every other call site in this pass. The end state: EVERY 'Vortex is open, can't do this yet' moment in this app shows the same real modal, consistently -- no quiet inline exceptions anywhere.\n\nVERIFY LIVE for both the retry fix AND the modal-consistency conversion, per file -- trigger the real condition, confirm the real modal shows (not an inline line), confirm Try Again actually retries and completes once Vortex is closed. Handoff with what you tested and a plain list of every spot that got converted from inline-text to modal, per file.",
      "ready": true
    },
    {
      "id": "rulesgen-unresolved-rule-no-detail",
      "tag": "bug",
      "title": "Rules Generator: phantom \"(unknown)\" rules that don't exist in Vortex's own current data",
      "why": "Director confirmed live, directly in Vortex's own 'Manage rules' dialog: for both real repro mods (Praedy's College of Winterhold 2K, Exist's Caves PBR - 2K), Vortex itself shows only ONE real rule -- but this app's own report shows a SECOND phantom \"(unknown)\" rule Vortex doesn't have at all. This isn't a missing-detail problem, it's reading a rule that may no longer be real/current.",
      "prompt": "REAL BUG, director's own live investigation directly in Vortex (screenshots): for TWO separate real repro mods -- \"Praedy's College of Winterhold 2K\" and \"Exist's Caves PBR - 2k\" -- Vortex's own real 'Manage rules' dialog shows only ONE real, current rule (e.g. \"after Faultier's PBR Landscapes 2k\"). But this app's own 'Current rules for X' report shows TWO: the real one, PLUS a phantom \"after (unknown)\" rule that doesn't exist in Vortex's current data at all.\n\nThis changes the original framing of this task (it started as 'show detail for the unresolved rule' -- that's now confirmed wrong; the real issue is this app is surfacing a rule that may not be real/current at all, not just failing to name a real one).\n\nINVESTIGATE LIVE, root cause -- don't guess: read the real state.v2 rules data for one of these two exact repro mods directly (same read path this app's own rule-generator code uses -- `lib/rules-generator.js`'s `getOldModRuleSet`/`resolveRefToModKey`) and compare it against what Vortex's own UI currently shows. Real candidate causes to check, not assume which:\n  (a) a genuinely STALE/orphaned rule entry still sitting in Vortex's raw state.v2 data, pointing at a mod that was since removed/uninstalled -- Vortex's own UI might silently filter these out of its 'Manage rules' display while this app's own raw read doesn't.\n  (b) a real parsing/matching bug in THIS app's own code that's fabricating or duplicating a rule entry that was never real to begin with.\n  (c) a rule referencing a DIFFERENT mod entirely that Vortex's UI groups/displays differently than this app expects (e.g. a rule on a related/hidden companion mod, not literally 'this exact mod' the way Vortex's dialog title implies).\n\nFIX, per the director's own explicit fallback direction if root cause can't be cleanly resolved: **if a rule's target genuinely cannot be resolved to anything real, don't surface it as a countable/actionable item at all** -- not as 'couldn't be matched,' not as '(unknown)' -- just silently exclude it from `rulesToConsider`/the summary count entirely. This is a real behavior change from the original task's own direction (which asked to surface MORE detail for these) -- the new, corrected instruction is the opposite: if it's genuinely not real/current, drop it silently rather than showing it at all. Only keep surfacing a rule as 'couldn't be matched' if it's a REAL, current rule (confirmed to exist in Vortex's own data) whose target just doesn't have a counterpart in the new collection -- that's a genuinely different, still-real case worth keeping visible.\n\nVERIFY LIVE against both real repro mods (Praedy's College of Winterhold 2K, Exist's Caves PBR - 2k) -- confirm the report's own rule count/detail now matches what Vortex's own 'Manage rules' dialog genuinely shows for each, no phantom '(unknown)' entries. If you find a real, still-current rule elsewhere that genuinely can't be matched to the new collection, confirm THAT case still shows correctly (not silently dropped -- only the phantom/stale case should be). Handoff with the real root cause you found for the phantom entries.",
      "ready": true
    }
  ],
  "roadmap": [
    {
      "title": "Theming framework (Phase 1)",
      "note": "The actual data-driven refactor (themes/plain.json + stable-ID runtime loader, per DESIGN.md's own \"Brand theming framework\" section) was only ever designed, never built -- was missing from the roadmap entirely. Build first, validate it changes nothing visible, before Phase 2.",
      "doc": "DESIGN.md"
    },
    {
      "title": "Theming framework (Phase 2)",
      "note": "First game theme (Skyrim, content drafted + kept current) -- depends on Phase 1 landing and being validated first.",
      "doc": "design/theme-content-skyrim.md"
    },
    {
      "title": "Harmonization (Phase 3)",
      "note": "back-port The Forge patterns + multi-select filter sweep",
      "doc": "DESIGN.md"
    },
    {
      "title": "Dynamic dashboard (Normal / Developer mode)",
      "note": "customizable per audience"
    }
  ],
  "shipped": [
    {
      "title": "ESLifier awareness — Settings path + Missing Masters downgrade",
      "doc": null
    },
    {
      "title": "Rebuild Missing Files' Extract action fails for any file nested under a stripped prefix",
      "doc": null
    },
    {
      "title": "Rebuild Missing Files: honest 'wrong version downloaded' vs 'genuinely missing' archive message",
      "doc": null
    },
    {
      "title": "New tool: Rebuild Missing Files — check picked collections, fix only what's actually missing",
      "doc": null
    },
    {
      "title": "Forge: friendly merge errors (no raw xelib/EPERM dumps) + cleanup hardening",
      "doc": null
    },
    {
      "title": "Rebuilding mods… needs an X-of-X counter and a % complete indicator",
      "doc": null
    },
    {
      "title": "The Forge — Merge Plugins",
      "doc": "https://github.com/awesmdiver/vortex-collection-tools/releases/tag/v0.5.0"
    },
    {
      "title": "Update Collection → step-by-step flow",
      "doc": "DESIGN.md"
    },
    {
      "title": "v0.5.0 release (pre-release, The Forge)",
      "doc": "https://github.com/awesmdiver/vortex-collection-tools/releases/tag/v0.5.0"
    },
    {
      "title": "GitHub kebab-case renames + private workspace meta-repo"
    },
    {
      "title": "Rebuild Missing Files: \"Refresh from Nexus\" per-card action"
    },
    {
      "title": "Rebuild Missing Files: read Vortex's DB so Workshop collections with no local collection.json show up at all"
    },
    {
      "title": "Rebuild Missing Files: replace per-card Refresh-from-Nexus with a single Workshop-only batch toggle"
    },
    {
      "title": "Clear stale end-of-flow banner on Preview re-arm"
    },
    {
      "title": "Rebuild Missing Files: add live progress (X of X) to the Extract action"
    },
    {
      "title": "New Reports tab: Workshop Report (per-collection Nexus timestamps + staging folder link)"
    },
    {
      "title": "Workshop Report: per-row Fetch from Nexus (does NOT fix Merge Plugins -- see merge-plugins-workshop-collection-blindspot)"
    },
    {
      "title": "Merge Plugins/Update Collection/Rules Generator: fixed to see Workshop-authored collections with real content"
    },
    {
      "title": "Rules Generator: dedup Workshop collections that now appear in both the old and new pickers"
    },
    {
      "title": "Rebuild Missing Files report: add Missing/Archive Issue filter badges + Show all"
    },
    {
      "title": "Rebuild Missing Files: show a mod as Ignored (grey, reference-only) if Vortex has it marked Ignored"
    },
    {
      "title": "Rebuild Missing Files: show \"Last dealt with\" per collection, like Rebuild Collection's Last extracted"
    },
    {
      "title": "Workshop Report: sortable \"Last updated on Nexus\" column (newest↔oldest)"
    },
    {
      "title": "Rebuild Missing Files: \"Last checked\" timestamp (and \"Last dealt with\" renamed to \"Last extracted\")"
    },
    {
      "title": "Rebuild Missing Files: don't flag a Vortex-Ghosted file as missing"
    },
    {
      "title": "Add a mod exception list -- skip hand-pick-only mods in Rebuild Collection and Rebuild Missing Files"
    },
    {
      "title": "Mod Exceptions report shows a stale list after navigating back, and needs a View on Nexus link"
    },
    {
      "title": "Rebuild Missing Files: fixed broken 'Try Again' on the Vortex-running modal (all 10 call sites)"
    }
  ]
}
